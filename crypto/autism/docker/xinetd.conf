service srv
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = autism
    type        = UNLISTED
    port        = 1337
    bind        = 0.0.0.0
    server      = /home/autism/autism
    per_source	= 5
    rlimit_cpu	= 10
}
