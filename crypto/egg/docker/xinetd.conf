service srv
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = egg
    type        = UNLISTED
    port        = 1337
    bind        = 0.0.0.0
    server      = /home/egg/egg.py
    per_source	= 5
    rlimit_cpu	= 10
    log_on_failure = HOST
}
